import { Client, ServiceOrder } from '../types';

// Somente dados reais inseridos pelo usuário são mantidos.
export const INITIAL_CLIENTS: Client[] = [];

export const INITIAL_ORDERS: ServiceOrder[] = [];
