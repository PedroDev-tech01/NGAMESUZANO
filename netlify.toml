# Configuração de Deploy no Netlify
[build]
  command = "npm run build"
  publish = "dist"

# Redirecionamento SPA para React Router / Vite
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
